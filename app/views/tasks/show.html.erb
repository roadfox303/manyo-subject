<h1>タスク詳細</h1>
<ul>
  <li>
      <h3>タスク名</h3>
      <%= @task.title %>
  </li>
  <li>
      <h3>タスク詳細</h3>
      <%= @task.comment %>
  </li>
</ul>
<%= link_to "編集", edit_task_path(@task.id) %>
<%= link_to "削除", task_path(@task.id), method: :delete %>
<%= link_to "一覧に戻る", tasks_path %>
