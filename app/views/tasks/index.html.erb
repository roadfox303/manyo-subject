<h1><%= t('view.tasks.title_index') %></h1>
<%= flash[:success] %>
<%= flash[:failure] %>
<dl>
<% @tasks.each do |task| %>
  <dt><%= link_to task.title, task_path(task.id) %></dt>
<% end %>
</dl>
<%= link_to t('view.tasks.link.create'), new_task_path %>
